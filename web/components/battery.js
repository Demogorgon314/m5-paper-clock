export const batteryComponent = Object.freeze({
  type: "battery",
  labelKey: "layout.component.battery",
  defaultItem: Object.freeze({
    id: "battery-main",
    type: "battery",
    labelKey: "layout.component.battery",
    x: 768,
    y: 20,
    w: 176,
    h: 44,
    props: Object.freeze({ variant: "status" }),
  }),
  propsSchema: Object.freeze([]),
});

