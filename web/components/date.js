export const dateComponent = Object.freeze({
  type: "date",
  labelKey: "layout.component.date",
  defaultItem: Object.freeze({
    id: "date-main",
    type: "date",
    labelKey: "layout.component.date",
    x: 24,
    y: 20,
    w: 744,
    h: 44,
    props: Object.freeze({ variant: "header" }),
  }),
  propsSchema: Object.freeze([]),
});

